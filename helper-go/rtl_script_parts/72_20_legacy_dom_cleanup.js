  document.getElementById(STYLE_ELEMENT_ID)?.remove();
  document.getElementById("agents-rtl-dry-draggable-style")?.remove();
  document.getElementById("agents-rtl-dry-draggable-cluster")?.remove();
  document.getElementById("agents-rtl-toolbar-slot-dry-style")?.remove();
  document.getElementById("agents-rtl-inline-toolbar-dry-style")?.remove();
  document.getElementById("agents-rtl-toolbar-middle-dry-style")?.remove();
  document.getElementById("agents-rtl-no-horizontal-scroll-dry-style")?.remove();
  document.getElementById("agents-rtl-final-layout-dry-style")?.remove();
  document.getElementById("agents-rtl-native-layout-dry-style")?.remove();
  document.getElementById("agents-rtl-antigravity-native-dry-style")?.remove();
  document.getElementById("agents-rtl-antigravity-scrollbar-edge-dry-style")?.remove();
  document.getElementById("agents-rtl-composer-dry-style")?.remove();
  document.getElementById("agents-rtl-table-dry-style")?.remove();
  document.getElementById("agents-rtl-gemini-dry-style")?.remove();
  document.getElementById("agents-rtl-response-annotation-rtl-dry-style")?.remove();
  document.getElementById("agents-rtl-reply-editor-background-dry")?.remove();
  document.getElementById("agents-rtl-annotation-kind-dry")?.remove();
  document.getElementById("agents-rtl-annotation-pill-purple-dry")?.remove();
  document.getElementById("agents-rtl-toolbar-slot")?.remove();
  document.getElementById("agents-rtl-inline-toolbar-controls")?.remove();
  document.getElementById("agents-rtl-gemini-dry-control-bar")?.remove();
  document.getElementById("agents-rtl-gemini-dry-bottom-slot")?.remove();
  document.getElementById("agents-rtl-native-inline-host")?.remove();
  document.getElementById("agents-rtl-native-inline-controls")?.remove();
  document.getElementById("agents-rtl-edge-scroll-up")?.remove();
  document.getElementById("agents-rtl-edge-scroll-down")?.remove();
  document.getElementById("agents-rtl-native-edge-up")?.remove();
  document.getElementById("agents-rtl-native-edge-down")?.remove();
  document.getElementById("agents-rtl-antigravity-inline-controls")?.remove();
  document.getElementById("agents-rtl-antigravity-edge-up")?.remove();
  document.getElementById("agents-rtl-antigravity-edge-down")?.remove();
  document.getElementById(BUTTON_CLUSTER_ID)?.remove();
  document.getElementById(CONTROL_HOST_ID)?.remove();
  document.getElementById(GEMINI_CONTROL_SLOT_ID)?.remove();
  document.getElementById(CONTROL_BAR_ID)?.remove();
  document.getElementById(CODEX_CHAT_TABS_BAR_ID)?.remove();
  document.getElementById(CODEX_CHAT_ACTIVE_ACTIONS_ID)?.remove();
  document.getElementById(CODEX_CHAT_OVERFLOW_MENU_ID)?.remove();
  document.getElementById(CODEX_CHAT_RENAME_EDITOR_ID)?.remove();
  document.getElementById(CODEX_CHAT_CONFIRMATION_ID)?.remove();
  removeCodexConversationListRenameControls();
  document.getElementById(SCROLL_UP_BUTTON_ID)?.remove();
  document.getElementById(SCROLL_DOWN_BUTTON_ID)?.remove();
